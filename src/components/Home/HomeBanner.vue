<template>
  <Swiper class="h-auto sm:h-[640px]" effect="fade" :modules="Modules" :autoplay="true && { delay: 3000 }" :loop="true"
    :navigation="{ prevEl: '.swiper-button-prev', nextEl: '.swiper-button-next' }" :pagination="{ type: 'fraction' }"
    @slideChange="onSlideChange()">
    <SwiperSlide v-for="(e, index) in 5" :key="index" class="relative">
      <img :src="require(`@/assets/images (1)/banner-${index + 1}.png`)" alt="e" class="w-full h-full">
      <div class="absolute left-2/4 top-2/4 -translate-x-2/4 -translate-y-2/4 text-white">
        <h3 class="text-base sm:text-3xl lg:text-5xl wow animate__fadeInDown text-center" data-wow-duration="1s">강조하는 제목{{
          index + 1 }}</h3>
        <p data-wow-duration="1s" class="wow animate__fadeInUp text-sm lg:text-2xl sm:text-xl text-center font-bold mt-4">
          부가설명</p>
        <p data-wow-duration="1s" class="wow animate__fadeInUp text-sm lg:text-2xl sm:text-xl text-center font-bold ">추가내용
        </p>
        <router-link to="/" data-wow-duration="1s"
          class="bg-slate-400 px-10 py-2 flex mx-auto w-32 mt-4 rounded-lg wow animate__fadeInUp">more<font-awesome-icon icon="plus"/></router-link>
      </div>
    </SwiperSlide>
    <div class="swiper-button-prev"></div>
    <div class="swiper-button-next"></div>
  </Swiper>
</template>

<script>
import { Swiper, SwiperSlide } from 'swiper/vue'
import { Navigation, EffectFade, Autoplay, Pagination } from 'swiper';

import 'swiper/css'
import 'swiper/css/bundle'
import 'swiper/css/pagination'
import 'swiper/css/navigation'

import WOW from 'wowjs'
import 'animate.css'

export default {
  name: "HomeBanner",
  data() {
    return {
      Modules: [Navigation, EffectFade, Autoplay, Pagination]
    }
  },
  components: {
    Swiper,
    SwiperSlide
  },
  mounted() {
    new WOW.WOW({
      boxClass: 'wow',
      animateClass: "animate__animated",
      live: false,
      mobile: true
    }).init();
  },
  methods: {
    onSlideChange() {
      new WOW.WOW({
        live: false
      }).init()
    }
  }
}
</script>

<style>
.swiper-pagination.swiper-pagination-fraction {
  background: rgba(255, 255, 255, 0.5);
  width: auto;
  left: 50%;
  transform: translateX(-50%);
  padding: 2px 35px;
  letter-spacing: 3px;
  border-radius: 20px;
  font-weight: bold;
}

@media screen and (max-width: 1024px) {

  .swiper-pagination.swiper-pagination-fraction,
  .swiper-pagination.swiper-pagination-fraction span {
    font-size: 14px;
  }
}

@media screen and (max-width: 640px) {

  .swiper-pagination.swiper-pagination-fraction,
  .swiper-pagination.swiper-pagination-fraction span {
    font-size: 10px;
  }

  .swiper-button-next::after,
  .swiper-button-prev::after {
    font-size: 14px;
  }
}

.swiper-button-next::after,
.swiper-button-prev::after {
  color: white;
}</style>